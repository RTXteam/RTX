apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arax.fullname" . }}-test-connection"
  labels:
    {{- include "arax.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arax.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
