apiVersion: v1
kind: Secret
metadata: 
  name: {{ include "arax.fullname" . }}-secret
  labels:
    {{- include "arax.labels" . | nindent 4 }}
data:
  {{- (.Files.Glob "secret/*.secret").AsConfig | nindent 2 }}
